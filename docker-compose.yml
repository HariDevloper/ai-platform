services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app/storage:/app/backend/app/storage
    environment:
      - DATABASE_URL=sqlite:///./app.db
      - APP_ENV=production
